<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.cat.recruit.mapper.AdminMapper">
    <select id="selectByUserName" resultType="com.cat.recruit.entity.Admin">
        SELECT * FROM governor WHERE user_name = #{userName}
    </select>
</mapper>